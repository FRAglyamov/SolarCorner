@page "/astronomy"
@using Astronomy

<h3>Astronomy</h3>
<p>Current time: @curTime.Hour:@curTime.Minute:@curTime.Second</p>

<p>Выдача местонахождения планет</p>

@code {
    DateTime curTime = DateTime.Now;
    SunTimes MoscowSunTimes = SunCalculator.GetTimes(DateTime.Now, 55.75700, 37.61500);
    SunTimes KazanSunTimes = SunCalculator.GetTimes(DateTime.Now, 55.80030, 49.10827);

}
